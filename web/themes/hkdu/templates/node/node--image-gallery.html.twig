<article{{ attributes.addClass(classes) }}>
  <div class="gFJypg">
    <div class="cgfNGU">{{ node.field_date.value }} at {{ node.field_venue.value }}</div>
    <div class="hRFBJC">{{ node.body.value|raw }}</div>
  </div>
  <div class="dAtiMd">
    {% for img in node.field_media %}
      <div class="ckWGLZ">
        <a class="gallery-group" data-lightbox="example-set" data-title="{{ img.entity.field_caption.value }}" href="{{ img.entity.field_media_image.entity.uri.value | image_style('original') }}">
          <div style="background-image: url({{ img.entity.field_media_image.entity.uri.value | image_style('max_325x325') }})"
               class="dZXNdD"></div>
        </a>
        <div class="biunsS">{{ img.entity.field_media_image.title }}</div>
      </div>

    {% endfor %}
  </div>
</article>